# remote access update
remote access greyed out instead of missing
remote access accessible even when your balance is 0 after first deposit
add info about remote access to the welcome letter

# Loans
- repo man

# modifications
- galactic bank branch offices only when vault is operational
  - $MajorFactionList.{$vault.sector.owner} to false when a vault is not operational.
  - $MajorFactionList.{$vault.sector.owner} to true when rebuilt station isoperational.

# Piracy
- add credit loot boxes for destroyed vaults (solid storage modules)
add drop to component macro? check if component destroyed?

# Player gets hunted
- banking clan associated factions hunt player when -20